import styled from "styled-components";

const LogoutBtn = styled.i`
  margin-left: 0.4rem;
  margin-right: -32rem;
  position: absolute;
  right: 35rem;
  top: 2.5rem;
  cursor: pointer;
`;

export { LogoutBtn };
