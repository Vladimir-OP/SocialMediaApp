import styled from "styled-components";

const LogoutBtn = styled.button`
  font-size: 1.75rem;
  margin-left: 0.4rem;
  margin-right: -32rem;
  position: absolute;
  right: 35rem;
  top: 1.5rem;
  cursor: pointer;
  background: none;
	border: none;
	padding: 0;
	outline: inherit;
`;

export { LogoutBtn };
