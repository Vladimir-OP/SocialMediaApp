import styled from "styled-components";

const LogoutBtn = styled.i`
font-size: 2rem;
  margin-left: 0.4rem;
  margin-right: -32rem;
  position: absolute;
  right: 35rem;
  top: 1.5rem;
  cursor: pointer;
`;

export { LogoutBtn };
